#!/usr/bin/env node

const { devtoolsPath } = require("../")
const path = require('path');

const dest = path.join(process.cwd(), "node_modules/@hap-toolkit/debugger/lib/client/html/inspector")
console.log(`sync from ${devtoolsPath} to ${dest}`)
require('sync-directory')(devtoolsPath, dest)
